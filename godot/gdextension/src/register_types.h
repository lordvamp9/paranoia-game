// PARANOIA GDExtension — module registration entry points.
#pragma once
#include <godot_cpp/core/class_db.hpp>

void initialize_paranoia_module(godot::ModuleInitializationLevel p_level);
void uninitialize_paranoia_module(godot::ModuleInitializationLevel p_level);
